/**
 * Asset types and loaders. Resolve assets by ID via Electron (list/create/update/delete).
 * Sample assets live under assets/samples/ (avatars, cubemaps, patterns).
 */

export type { AssetMeta, AvatarAsset, PatternAsset, MaterialAsset, AssetType } from './types';
